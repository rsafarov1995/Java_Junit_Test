# Java_Junit_Test

»зготовить аналог библиотеки JUnit.
Ќеобходимо реализовать следующее API - аннотаци€ @Test с 
опциональным атрибутом ожидаемого исключени€, 
аннотации @Before, @After, свои методы проверки условий (ассЄрты).

—истема должна работать на множестве потоков.
 аждый поток должен после запуска в бесконечном цикле:
1) ожидать поступлени€ в некоторой общей очереди нового имени класса с тестами
2) выполн€ть эти тесты
3) возвращать отчЄт о прошедших/упавших тестах

«апуск тестов должен осуществл€тьс€ следующим командным интерфейсом:

java -cp <your-junit-jar>;<tested-classes> <your-main-class> N class-name [class-name]*

N - количество потоков, в которых должны запускатьс€ тесты.
ќстальные аргументы (произвольное количество) - полные имена классов, в которых наход€тс€ тесты.